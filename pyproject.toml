[project]
name = "broadtherm"

version = "0.1.0"

description = "Turn any infrared air conditioner into a smart thermostat."

authors = [
    {name="Lionel D'Anna"}
]

license = {text = "MIT"}

requires-python = ">=3.10"

dependencies = [
    "broadlink>=0.19.0"
]
